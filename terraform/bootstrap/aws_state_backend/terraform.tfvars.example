aws_region  = "us-east-1"
name_prefix = "mrh-jishan-task-manager-terraform-state"
# Leave bucket_name unset unless you already own a globally unique S3 bucket name.
# bucket_name = "some-globally-unique-name"

tags = {
  Project   = "task-manager"
  ManagedBy = "terraform"
}
