replicaCount: 1

resources:
  requests:
    cpu: 150m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi

serviceAccount:
  name: task-manager-backend

secretsStore:
  enabled: true

persistence:
  enabled: true
  size: 10Gi
  storageClass: gp2
