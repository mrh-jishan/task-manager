{{- if and .Values.backend.secretsStore.enabled .Values.backend.secretsStore.secretArn .Values.backend.secretsStore.region }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "task-manager.backendSecretProviderClassName" . }}
  labels:
    {{- include "task-manager.backendLabels" . | nindent 4 }}
spec:
  provider: aws
  parameters:
    region: {{ .Values.backend.secretsStore.region | quote }}
    objects: |
      - objectName: {{ .Values.backend.secretsStore.secretArn | quote }}
        objectType: "secretsmanager"
        jmesPath:
{{- range .Values.backend.secretsStore.jmesPath }}
          - path: {{ . | quote }}
            objectAlias: {{ . | quote }}
{{- end }}
{{- end }}
